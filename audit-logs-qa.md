# Audit logs QA record

## Founder desktop — 2026-08-23

Founder successfully opened `/admin/audit-logs` in the protected dashboard. The fixed sidebar displayed **Dashboard**, **Users** and **Audit logs**; the audit route was active. The page rendered the active/archive switch, manual 12-month archive control, global search, role/action/object/result and UTC date filters, page-size selector, and CSV/PDF controls.

The initial audit table loaded 14 real events generated by test-router interactions. It displayed local time plus UTC, actor ID/role, action, target object, IP, browser/OS and result. No fictional rows were inserted. The logged unit-test request contexts correctly appear with unknown client platform values rather than fabricated browser or IP data.

Founder entered the partial query `user`. The page rendered server-backed suggestion options for matching IDs, action codes, target types and descriptions. The filter did not create business data; it only produced the expected self-audit interaction event.

After the interaction queries settled, the Founder table showed the expected fresh `audit.view` and `audit.search` events with the authenticated actor ID, derived source IP, Google Chrome and Linux labels. This confirms that audit-module interactions self-log without infinite writer recursion. The event list remained within the page-size bound.

## Super admin self-cleaning browser QA — 2026-08-23

The self-cleaning QA created a temporary Super admin, signed in through universal login, opened `/super-admin/audit-logs`, and removed the account together with its temporary active/archive Audit rows in its `finally` cleanup. Four checks passed: the active scoped UI did not expose archive/manual archive or a Founder role filter; a reload displayed only the current Super admin’s interaction without Founder/private-control activity; a protected scoped CSV export downloaded successfully; and the 390×844 route retained controls without horizontal document overflow.

Founder archive-tab QA confirmed the Founder-only Archive tab and restore-selection guidance are present. With no eligible archived data, the interface rendered an explicit zero-record empty state rather than inserting sample rows.

## Responsive overflow remediation — 2026-08-23

At the constrained desktop workspace width, the active Founder list now renders compact, complete event cards instead of a horizontally overflowing wide table. The filter panel wraps into available columns, and the CSV/PDF actions, archive controls, filters, pagination and all event fields remain reachable without the right edge leaving the viewport. The full comparison table remains available on ultra-wide desktop screens.

The updated self-cleaning Super admin E2E passed again after the refactor: the responsive event-card representation exposed no Founder/private activity and no desktop document overflow; the scoped CSV flow still passed; and the 390×844 viewport still had no horizontal document overflow.

## Remaining browser checks

- Verify Founder archive tab, manual zero/eligible-record behaviour and restore selection with self-cleaning data only.
- Verify Super admin scoped page excludes Founder and peer Super admin records.
- Verify search suggestions, filter combinations, CSV/PDF downloads and desktop/mobile table overflow.
