import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, Eye, EyeOff, LockKeyhole, Mail, ShieldCheck } from "lucide-react";
import { FormEvent, useState } from "react";
import { Link, useLocation } from "wouter";

export default function FounderLogin() {
  const [, setLocation] = useLocation(); const utils = trpc.useUtils();
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [visible, setVisible] = useState(false);
  const status = trpc.auth.founderAuthStatus.useQuery();
  const login = trpc.auth.founderLogin.useMutation({ onSuccess: async () => { await utils.auth.me.invalidate(); setLocation("/admin"); } });
  function submit(event: FormEvent) { event.preventDefault(); login.mutate({ email, password }); }
  return <main className="grid min-h-screen bg-[#fbf8f2] lg:grid-cols-[1.05fr_.95fr]"><section className="hidden bg-[#10253e] p-12 text-white lg:flex lg:flex-col lg:justify-between"><Link href="/" className="inline-flex items-center gap-2 text-sm font-extrabold text-[#a7c6b8] hover:text-[#f3b59f]"><ArrowLeft size={16} /> Back to website</Link><div className="max-w-xl"><p className="text-xs font-extrabold tracking-[.15em] text-[#a7c6b8] uppercase">Bilingual Idol</p><h1 className="mt-5 font-display text-6xl leading-[.95]">Founder<br /><em className="font-normal text-[#f3b59f]">console.</em></h1><p className="mt-7 max-w-md text-base leading-7 text-white/70">A focused workspace for confirmed programmes, enquiries, team profiles, verified reviews, and centre announcements.</p></div><p className="text-xs font-semibold tracking-[.08em] text-white/45 uppercase">Secure access · no public registration</p></section><section className="flex items-center justify-center p-6 sm:p-10"><div className="w-full max-w-md"><Link href="/" className="inline-flex items-center gap-2 text-sm font-extrabold text-[#5e8c7b] hover:text-[#10253e] lg:hidden"><ArrowLeft size={16} /> Back to website</Link><div className="mt-10 rounded-[1.75rem] border border-[#e1d5c4] bg-white p-7 shadow-[0_16px_45px_rgba(16,37,62,.09)] sm:p-9"><span className="grid h-12 w-12 place-items-center rounded-full bg-[#e7f0eb] text-[#5e8c7b]"><ShieldCheck size={22} /></span><p className="mt-7 text-xs font-extrabold tracking-[.13em] text-[#5e8c7b] uppercase">Secure sign in</p><h2 className="mt-2 font-display text-4xl text-[#10253e]">Welcome back.</h2><p className="mt-3 text-sm leading-6 text-[#5b6d80]">Use the Founder e-mail and password. Registration is not available on this platform.</p>{status.data?.configured === false && <p className="mt-4 rounded-xl bg-[#fff0ed] p-3 text-sm text-[#a34732]" role="alert">Founder sign-in has not been configured yet.</p>}<form className="mt-7 grid gap-5" onSubmit={submit}><div className="grid gap-2"><Label htmlFor="founder-email">E-mail</Label><div className="relative"><Mail className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#708098]" size={17} /><Input id="founder-email" type="email" autoComplete="email" required className="h-12 pl-10" value={email} onChange={event => setEmail(event.target.value)} /></div></div><div className="grid gap-2"><Label htmlFor="founder-password">Password</Label><div className="relative"><LockKeyhole className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#708098]" size={17} /><Input id="founder-password" type={visible ? "text" : "password"} autoComplete="current-password" required minLength={1} className="h-12 pl-10 pr-11" value={password} onChange={event => setPassword(event.target.value)} /><button type="button" className="absolute right-3 top-1/2 -translate-y-1/2 text-[#708098] hover:text-[#10253e]" aria-label={visible ? "Hide password" : "Show password"} onClick={() => setVisible(!visible)}>{visible ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></div>{login.error && <p className="rounded-xl bg-[#fff0ed] p-3 text-sm text-[#a34732]" role="alert">Invalid e-mail or password.</p>}<Button type="submit" className="mt-1 h-12 bg-[#10253e] text-white hover:bg-[#f07e5d] hover:text-[#10253e]" disabled={login.isPending || status.data?.configured === false}>{login.isPending ? "Signing in…" : "Sign in"}</Button></form></div></div></section></main>;
}
