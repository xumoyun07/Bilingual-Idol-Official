import { ArrowUpRight, Filter, Search, SlidersHorizontal } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";
import { PublicLayout } from "@/components/PublicLayout";
import { PROGRAM_CATEGORIES } from "@/lib/siteData";
import { trpc } from "@/lib/trpc";

export default function Programs() {
  const [category, setCategory] = useState<(typeof PROGRAM_CATEGORIES)[number]>("All"); const [query, setQuery] = useState(""); const programs = trpc.content.publicPrograms.useQuery();
  const matches = useMemo(() => (programs.data ?? []).filter(item => (category === "All" || item.category === category) && [item.title, item.language, item.ageGroup, item.level].join(" ").toLowerCase().includes(query.toLowerCase())), [category, query, programs.data]);
  return <PublicLayout><section className="bg-[#10253e] py-16 text-white sm:py-20"><div className="site-container max-w-4xl"><p className="eyebrow !text-[#a7c6b8]">Programmes & courses</p><h1 className="mt-5 font-display text-5xl leading-[.98] sm:text-6xl">Find a path that feels like <em className="font-normal text-[#f3b59f]">yours.</em></h1><p className="mt-6 max-w-2xl text-base leading-7 text-white/70">Explore courses by age group, language, level, and learning goal. Staff enter each programme’s confirmed schedule and fee information before it appears here.</p></div></section><section className="surface-grid py-14 sm:py-20"><div className="site-container"><div className="rounded-[1.4rem] border border-[#e1d5c4] bg-white p-4 shadow-[0_12px_32px_rgba(16,37,62,.06)]"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-center gap-2 text-sm font-extrabold text-[#29415b]"><SlidersHorizontal size={18} className="text-[#5e8c7b]" /> Find your programme</div><label className="relative w-full lg:max-w-xs"><span className="sr-only">Search programmes</span><Search className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#708098]" size={17} /><input className="w-full rounded-xl border border-[#dfd1bf] py-2.5 pl-10 pr-3 text-sm outline-none focus:border-[#5e8c7b] focus:ring-2 focus:ring-[#5e8c7b]/20" placeholder="Search by language or level" value={query} onChange={event => setQuery(event.target.value)} /></label></div><div className="mt-4 flex flex-wrap gap-2" aria-label="Programme category filters">{PROGRAM_CATEGORIES.map(item => <button key={item} className={`rounded-full px-4 py-2 text-xs font-extrabold ${category === item ? "bg-[#10253e] text-white" : "bg-[#f7f1e8] text-[#53657a] hover:bg-[#e7f0eb]"}`} onClick={() => setCategory(item)}>{item}</button>)}</div></div><div className="mt-8 flex items-center justify-between gap-4"><p className="text-sm text-[#53657a]"><span className="font-extrabold text-[#10253e]">{matches.length}</span> programmes to explore</p><p className="hidden items-center gap-2 text-xs font-bold text-[#5e8c7b] sm:flex"><Filter size={14} /> Filtered by fit, not by pressure</p></div>{programs.isLoading ? <div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{[1,2,3].map(item => <div key={item} className="h-64 animate-pulse rounded-[1.35rem] bg-white" />)}</div> : <><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{matches.map(program => <Link key={program.slug} href={`/programs/${program.slug}`} className="group rounded-[1.35rem] border border-[#e1d5c4] bg-white p-5 shadow-[0_6px_24px_rgba(16,37,62,.04)] hover:-translate-y-1 hover:border-[#f07e5d]/70 hover:shadow-[0_20px_40px_rgba(16,37,62,.1)]"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-extrabold tracking-[.12em] text-[#5e8c7b] uppercase">{program.category}</p><p className="mt-2 text-xs font-semibold text-[#708098]">{program.language} · {program.level}</p></div><span className="grid h-9 w-9 place-items-center rounded-full bg-[#f7f1e8] text-[#10253e] group-hover:bg-[#f07e5d]"><ArrowUpRight size={16} /></span></div><h2 className="mt-8 font-display text-3xl leading-none text-[#10253e]">{program.title}</h2><p className="mt-4 text-sm leading-6 text-[#5b6d80]">{program.description}</p><p className="mt-5 border-t border-[#eee4d7] pt-4 text-xs font-bold text-[#53657a]">For: {program.ageGroup}</p></Link>)}</div>{matches.length === 0 && <div className="mt-8 rounded-[1.3rem] border border-dashed border-[#cbbba4] bg-[#fbf8f2] p-8 text-center"><p className="font-display text-3xl text-[#10253e]">Confirmed programme details are on their way.</p><p className="mt-2 text-sm text-[#53657a]">Contact the centre for a tailored recommendation while the catalogue is being updated.</p><Link href="/contact" className="mt-4 inline-flex text-sm font-extrabold text-[#5e8c7b] hover:text-[#10253e]">Ask for guidance <ArrowUpRight size={15} /></Link></div>}</>}</div></section></PublicLayout>;
}
